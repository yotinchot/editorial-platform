# db/

Drizzle ORM schema + client. Lands in Phase 2 (Database & Content Model).

Planned contents:
- `schema.ts` — `posts`, `categories`, `post_categories`, `admin_sessions`
- `index.ts` — Drizzle client bound to Vercel Postgres
- `migrations/` — generated by `drizzle-kit`

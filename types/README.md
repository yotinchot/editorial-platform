# types/

Cross-feature shared types only (e.g. inferred Drizzle row types once
`db/schema.ts` exists). Feature-local types stay next to their feature
(see `features/posts/types/`, `features/categories/types/`).
